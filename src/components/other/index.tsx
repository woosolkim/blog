import React from "react";
import styled from "@emotion/styled";

const Container = styled.div`
  width: 100%;
`;

const Other = () => {
  return <Container>다른페이지</Container>;
};

export default Other;
