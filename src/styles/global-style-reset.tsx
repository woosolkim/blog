import { css } from "@emotion/react";

const GlobalStyleReset = css`
  body {
    margin: 0;
  }

  * {
    box-sizing: border-box;
    background-color: transparent;
  }
`;
export default GlobalStyleReset;
